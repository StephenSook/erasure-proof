# web

The frontend: a React + Vite SPA. The landing page, the live demo console (the judged surface),
the public proof verifier, and the `/trust` honesty page. Motion uses anime.js v4 on a
warm-black + neon palette. Built loop-first (plain UI wired to the live loop), then styled.
